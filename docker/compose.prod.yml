name: meik-prod
services:
  web:
    build:
      target: prod
    volumes:
      - ./certificates:/certificates
    ports:
      - 443:443 # nur HTTPS
  
  app:
    build:
      target: prod
  
  node:
    build:
      target: prod
